// Placeholder App. Phase 0 / Phase 1 will replace this with real routing,
// providers, and the schema-driven LabPage. See REBUILD_SPEC.md sections 5.6, 6.

export function App() {
  return (
    <main className="placeholder">
      <h1>Interactive Physics Labs</h1>
      <p>
        Scaffold up. Routing, providers, and the schema-driven renderer come online in
        Phase 0 / Phase 1. See <code>REBUILD_SPEC.md</code>.
      </p>
    </main>
  );
}
